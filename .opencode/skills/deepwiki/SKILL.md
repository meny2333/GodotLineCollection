---
name: deepwiki
description: Use when needing to understand any part of the GodotLineCollection project - architecture, data flow, code mapping, or subsystem behavior. Check deepwiki before reading source code.
---

先用 `read_wiki_structure` + `read_wiki_contents` 查 `meny2333/GodotLineCollection` 的 wiki，再读源码。Wiki 覆盖架构、数据流、代码映射。
